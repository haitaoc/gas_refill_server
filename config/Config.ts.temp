const config = {
    server: {
        host: 'localhost',
        port: 3000
    },
    database: {
        dynamodb: {
            accessId: '',
            accessKey: '',
            region: 'us-west-2',
        }
    },
    corsOptions: {
        origin: "http://localhost:3000"
    }
};

export default config;